"""Degradation indicators and the health index built on them.

A failure mode is a distinct way a machine can fail, measured by its own number,
reaching failure at its own value. They live in app.failure_modes rather than in
this package, so adding one is a database row.
"""
